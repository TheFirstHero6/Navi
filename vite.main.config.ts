import { defineConfig } from 'vite';

export default defineConfig({
  // Forge Vite plugin handles the build configuration
  // This file is mainly for any custom Vite settings for the main process
});

